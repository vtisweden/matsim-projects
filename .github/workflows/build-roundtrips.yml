
    name: Build roundtrips

    on: [pull_request, workflow_dispatch]

    jobs:
      call-build:
        uses: ./.github/workflows/build-module.yml
        with:
          module: 'roundtrips'
    